"use strict";

// alert(3 + 3);

// Premetive DataTypes
let age = 18;
let name = "Uday";
let bool = true;
let temp = null;
let marks;

console.table([age, name, bool, temp, marks]);

// now we use typeof by consoling to check;
console.log(typeof age); // returns number
console.log(typeof name); // returns string
console.log(typeof bool); // returns boolean
console.log(typeof temp); // returns object
console.log(typeof marks); // returns  undefined
